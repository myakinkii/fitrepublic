{
  "name": "fitrepublic",
  "version": "1.0.0",
  "description": "Generated by cds init",
  "repository": "<Add your repository here>",
  "license": "UNLICENSED",
  "dependencies": {
    "@sap/cds": "^6",
    "@sap/cds-odata-v2-adapter-proxy": "^1.9.3",
    "express": "^4",
    "uuid": "^8.3.2"
  },
  "devDependencies": {
    "@ui5/cli": "^2.14.10",
    "sqlite3": "^5.0.4",
    "ui5-middleware-simpleproxy": "^0.9.0"
  },
  "scripts": {
    "build": "cds build",
    "_deploy": "cds deploy",
    "start": "cds run",
    "memory": "cds run --in-memory",
    "local": " CDS_ENV=local cds run",
    "deploy": "cds deploy --to sqlite:my.db --no-save"
  },
  "cds": {
    "[local]": {
      "requires": {
        "db": {
          "kind": "sqlite",
          "credentials": {
            "database": "my.db"
          }
        }
      }
    },
    "requires": {
      "db": {
        "kind": "hana",
        "model": [
          "db",
          "srv"
        ]
      }
    },
    "odata": {
      "version": "v4"
    }
  },
  "files": [
    "db",
    "srv"
  ]
}