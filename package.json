{
  "name": "fitrepublic",
  "version": "1.0.0",
  "description": "Generated by cds init",
  "repository": "<Add your repository here>",
  "license": "UNLICENSED",
  "dependencies": {
    "@cap-js/sqlite": "^1.5.1",
    "@sap/cds": "^7.7.0",
    "@sap/cds-odata-v2-adapter-proxy": "^1.9.3",
    "express": "^4",
    "uuid": "^8.3.2"
  },
  "devDependencies": {
    "@ui5/cli": "^2.14.10",
    "ui5-middleware-simpleproxy": "^0.9.0"
  },
  "scripts": {
    "build": "cds build",
    "deploy": "cds deploy",
    "deploy:model": "cds deploy --model-only",
    "start": "cds run",
    "memory": "cds run --in-memory",
    "local": "cds run",
    "_deploy": "cds deploy --to sqlite:my.db --no-save"
  },
  "cds": {
    "requires": {
      "db": {
        "kind": "sqlite",
        "schema_evolution": "auto",
        "credentials": {
          "database": "my.db"
        }
      }
    },
    "odata": {
      "version": "v4"
    }
  },
  "files": [
    "db",
    "srv"
  ]
}